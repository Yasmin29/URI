//Calculo Simples
#include <iostream>
#include <iomanip>
#include <cmath>
using namespace std;

int main() {
    double a,b,c,s,t,r;
    cin >> a >> b >> c;
    cout << fixed << setprecision (1);

    s = pow(b + sqrt(c+2)+1,2);
    R
  //  cout << s << endl;

    return 0;

}
