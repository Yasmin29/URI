#include <iostream>

using namespace std;

int main() {
    int x;
    int par[5];
    int impar[5];
   for (int i=0; i<15; i++) {
        cin >> x;



/*        for (int i2=0; i<5; i++)
        if( x%2 != 0) {
        impar[i2] =x;
        }
        for (int i3=0; i<5; i++)
        if ( x%2 == 0) {
        par[i3] = x;
        cout << "par[" << i3 << "] = " << x << endl;
   }
}
       // cout << "par[" <<x[i3] << "] = " << x << endl; */
    return 0;
}
