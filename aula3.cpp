#include <iostream>
#include <cmath>

using namespace std;

int main () {
    int a,b,c,x,y,rl;

    cin >> rl;

    x=(pow(y,2));
    x=4/(2*a);
    x= (sqtr(pow(y,2)+2);
    x=b*h/(2* (4.a.c));
    rl=(-b sqrt( + pow(b*2) -4 * a * c)) 2*a;

    return 0;
}
